name: digit-lab
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - Flask>=3.0,<4
      - torch>=2.5
      - torchvision>=0.20
      - Pillow>=10
      - reportlab>=4
